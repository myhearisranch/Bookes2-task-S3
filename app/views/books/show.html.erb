<div class="post-body">
  <%= attachment_image_tag @book_image, :image %>
  <p>ショップ名：<%= @book_image.text %></p>
  <p>ユーザーネーム：<%= @book_image.user.name %></p>
  <p>投稿日時：<%= @book_image.created_at.strftime('%Y/%m/%d') %></p>
</div>