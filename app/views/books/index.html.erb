<% @book_images.each do |book_image| %>
<div>
 <%= attachment_image_tag book_image, :image %>
  <p>投稿ユーザー画像</p>
  <p>本のタイトル：<%= book_image.title %></p>
  <p>感想：<%= book_image.body%></p>
</div>
<% end %>